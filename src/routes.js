import {
    Switch as Switch,
    Route as Route,
    Router as Router,

} from 'react-router-dom';



export default (
    <Switch>
        <Route component={}/>
        <Route component={}/>
        <Route component={}/>
    </Switch>
)